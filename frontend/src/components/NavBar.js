import { AppBar, Button, Toolbar } from "@mui/material";

const NavBar = () => {
  return (
    <div>
      <AppBar>
        <Toolbar>
          
        </Toolbar>
      </AppBar>
    </div>
  );
};

export default NavBar;
